import {Test, console} from "forge-std/Test.sol";

contract LendRewardSplitterGovClaimTest is Test {
    //TODO:
    // function test_ProcessGovRewardsWithClaim() external {
    //     _processGovReward(false);
    //     // uint256 gUSDBal = gUSD.balanceOf(depositor);
    //     // console.log("gUSDBal before", gUSD.balanceOf(depositor));
    //     skip(3 weeks);
    //     gUSD.getReward(depositor);
    //     gUSD.getReward(depositor);
    //     IERC20 CRV = IERC20(Addresses.TOKEN_CRV);
    //     // console.log("gUSDBal after", gUSD.balanceOf(depositor));
    //     assertEq(crvClaimable, CRV.balanceOf(address(depositor)));
    // }
}
